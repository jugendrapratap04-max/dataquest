// Practice problems for the Python topics that had none.
//
// docs/LEARNING-SPEC.md §3 asks for at least two practice problems per topic,
// because a topic unlocks the next one only when its problems are solved. 20 of
// Python's 39 topics were below that bar — 13 with nothing at all — so the gate
// existed but had nothing to gate on.
//
// Originally Python only, on the rule that asking a student to practise a topic
// that has not been taught yet puts the gate before the lesson. Statistics now
// meets that condition — all 11 of its topics are at the full standard — so the
// four it was still short of two problems on are at the end of this file.
// The remaining seven subjects are still 48-129 word stubs and stay excluded.
//
// House rules, learned the hard way and enforced by `npm run db:check`, which
// runs every reference solution in Pyodide before anything ships:
//   - return scalars, strings or lists. NEVER a dict: it arrives in JS as a Map
//     and JSON.stringify(Map) is "{}", so grading silently passes everything.
//   - nothing random, nothing time-dependent, nothing with a memory address.
//   - Pyodide has no threads and no subprocess. Topics about those are practised
//     through the reasoning instead, which is the part an interview asks about.
//   - Pyodide DOES have a real in-memory filesystem, so file work is genuine.

const PP = (lessonSlug, difficulty, order, slug, title, functionName, desc, examples, starter, solution, tests, hints, tags) => ({
  lessonSlug, order, slug, title, difficulty, kind: "python",
  functionName, descriptionMd: desc, tagsCsv: tags.join(","),
  examplesJson: JSON.stringify(examples),
  starterCode: starter, solutionCode: solution, testsJson: JSON.stringify(tests),
  hintsJson: JSON.stringify(hints), sqlSetup: "",
  xp: difficulty === "Easy" ? 20 : difficulty === "Medium" ? 30 : 40,
});

const topicProblems = [
  /* ---------- getting-started ---------- */
  PP("getting-started", "Easy", 300, "first-greeting", "Your First Output", "greeting",
    "Write a function `greeting()` that returns the text `Hello, World!` exactly.\n\nThis is the first program almost every programmer writes.",
    [{ input: "greeting()", output: '"Hello, World!"' }],
    "def greeting():\n    pass\n",
    "def greeting():\n    return \"Hello, World!\"\n",
    [{ args: [], expected: "Hello, World!" }, { args: [], expected: "Hello, World!" }, { args: [], expected: "Hello, World!" }],
    ["Return the text, do not print it. The tests read what you return.", "The capital letters and the comma matter."],
    ["basics", "strings"]),
  PP("getting-started", "Easy", 301, "sum-three", "Add Three Numbers", "sum_three",
    "Write a function `sum_three(a, b, c)` that returns the total of the three numbers.",
    [{ input: "a=1, b=2, c=3", output: "6" }, { input: "a=0, b=0, c=0", output: "0" }],
    "def sum_three(a, b, c):\n    pass\n",
    "def sum_three(a, b, c):\n    return a + b + c\n",
    [{ args: [1, 2, 3], expected: 6 }, { args: [0, 0, 0], expected: 0 }, { args: [-5, 5, 10], expected: 10 }],
    ["Add all three with `+`.", "return a + b + c"],
    ["basics", "arithmetic"]),

  /* ---------- file-handling ---------- */
  PP("file-handling", "Easy", 302, "save-and-read", "Save It, Then Read It Back", "save_and_read",
    "Write a function `save_and_read(text)` that writes `text` into a file called `note.txt`, then opens that file, reads it back and returns what it read.\n\nYes, the file is real. Python in your browser has its own filesystem.",
    [{ input: 'text="hello"', output: '"hello"' }],
    "def save_and_read(text):\n    pass\n",
    "def save_and_read(text):\n    with open(\"note.txt\", \"w\") as f:\n        f.write(text)\n    with open(\"note.txt\") as f:\n        return f.read()\n",
    [{ args: ["hello"], expected: "hello" }, { args: [""], expected: "" }, { args: ["line one"], expected: "line one" }],
    ["Open with `\"w\"` to write, then open again with no mode to read.", "`with open(...) as f:` closes the file for you."],
    ["files"]),
  PP("file-handling", "Medium", 303, "count-saved-lines", "Count the Real Lines", "count_lines",
    "Write a function `count_lines(lines)` that writes the list into a file, one item per line, then reads the file back and returns how many lines actually have text in them.\n\nBlank lines do not count.",
    [{ input: 'lines=["a", "", "b"]', output: "2" }, { input: "lines=[]", output: "0" }],
    "def count_lines(lines):\n    pass\n",
    "def count_lines(lines):\n    with open(\"lines.txt\", \"w\") as f:\n        f.write(\"\\n\".join(lines))\n    with open(\"lines.txt\") as f:\n        return len([l for l in f.read().split(\"\\n\") if l.strip()])\n",
    [{ args: [["a", "", "b"]], expected: 2 }, { args: [[]], expected: 0 }, { args: [["x"]], expected: 1 }],
    ["`\"\\n\".join(lines)` turns the list into one block of text.", "After reading, split on `\"\\n\"` and skip anything whose `.strip()` is empty."],
    ["files", "strings"]),

  /* ---------- modules ---------- */
  PP("modules", "Easy", 304, "square-root", "Square Root", "root",
    "Import the `math` module and write a function `root(n)` that returns the square root of `n`, **rounded to 2 decimals**.",
    [{ input: "n=9", output: "3.0" }, { input: "n=2", output: "1.41" }],
    "import math\n\ndef root(n):\n    pass\n",
    "import math\n\ndef root(n):\n    return round(math.sqrt(n), 2)\n",
    [{ args: [9], expected: 3.0 }, { args: [2], expected: 1.41 }, { args: [0], expected: 0.0 }],
    ["`math.sqrt(n)` gives the square root.", "Wrap it in `round(x, 2)`."],
    ["modules", "math"]),
  PP("modules", "Medium", 305, "middle-value", "The Middle Value", "middle",
    "Use the `statistics` module. Write a function `middle(nums)` that returns the **median** of the list, rounded to 2 decimals.\n\nThe median is the middle value once the numbers are in order. The module already knows how.",
    [{ input: "nums=[1, 3, 2]", output: "2.0" }, { input: "nums=[1, 2, 3, 4]", output: "2.5" }],
    "import statistics\n\ndef middle(nums):\n    pass\n",
    "import statistics\n\ndef middle(nums):\n    return round(float(statistics.median(nums)), 2)\n",
    [{ args: [[1, 3, 2]], expected: 2.0 }, { args: [[1, 2, 3, 4]], expected: 2.5 }, { args: [[7]], expected: 7.0 }],
    ["`statistics.median(nums)` does the sorting for you.", "Wrap in `float(...)` then `round(x, 2)`."],
    ["modules", "statistics"]),

  /* ---------- scope ---------- */
  PP("scope", "Easy", 306, "running-total-scope", "A Total That Survives", "running_total",
    "Write a function `running_total(nums)` that adds up the list and returns the total.\n\nThe point of this one is where you set up `total`. Set it up **before** the loop, not inside it, or every pass throws away what came before.",
    [{ input: "nums=[5, 10, 15]", output: "30" }],
    "def running_total(nums):\n    pass\n",
    "def running_total(nums):\n    total = 0\n    for n in nums:\n        total += n\n    return total\n",
    [{ args: [[5, 10, 15]], expected: 30 }, { args: [[]], expected: 0 }, { args: [[-2, 2]], expected: 0 }],
    ["`total = 0` goes above the loop.", "Inside the loop, do `total += n`."],
    ["scope", "loops"]),
  PP("scope", "Medium", 307, "nonlocal-counter", "Count From the Inside", "count_hits",
    "Write a function `count_hits(nums, limit)` that counts how many numbers are greater than `limit`.\n\nDo the counting in a **nested function**, and use `nonlocal` so the inner function can update the outer one's variable.",
    [{ input: "nums=[1, 5, 9], limit=4", output: "2" }],
    "def count_hits(nums, limit):\n    pass\n",
    "def count_hits(nums, limit):\n    hits = 0\n\n    def check(n):\n        nonlocal hits\n        if n > limit:\n            hits += 1\n\n    for n in nums:\n        check(n)\n    return hits\n",
    [{ args: [[1, 5, 9], 4], expected: 2 }, { args: [[], 0], expected: 0 }, { args: [[1, 2], 10], expected: 0 }],
    ["Without `nonlocal`, assigning to `hits` inside makes a new local one.", "`nonlocal hits` tells Python you mean the outer variable."],
    ["scope", "functions"]),

  /* ---------- dates ---------- */
  PP("dates", "Easy", 308, "days-between", "Days Between Two Dates", "days_between",
    "Write a function `days_between(a, b)` that takes two dates as text in `YYYY-MM-DD` form and returns how many days apart they are.\n\nThe answer should never be negative.",
    [{ input: 'a="2026-01-01", b="2026-01-10"', output: "9" }],
    "from datetime import date\n\ndef days_between(a, b):\n    pass\n",
    "from datetime import date\n\ndef days_between(a, b):\n    d1 = date.fromisoformat(a)\n    d2 = date.fromisoformat(b)\n    return abs((d2 - d1).days)\n",
    [{ args: ["2026-01-01", "2026-01-10"], expected: 9 }, { args: ["2026-01-10", "2026-01-01"], expected: 9 }, { args: ["2026-03-01", "2026-03-01"], expected: 0 }],
    ["`date.fromisoformat(\"2026-01-01\")` reads that format directly.", "Subtracting two dates gives a timedelta; `.days` is the number you want.", "Use `abs(...)` so the order does not matter."],
    ["dates"]),
  PP("dates", "Medium", 309, "pretty-date", "Write the Date Nicely", "pretty_date",
    "Write a function `pretty_date(year, month, day)` that returns the date in the form `05 Jan 2026` — two-digit day, short month name, four-digit year.",
    [{ input: "year=2026, month=1, day=5", output: '"05 Jan 2026"' }],
    "from datetime import date\n\ndef pretty_date(year, month, day):\n    pass\n",
    "from datetime import date\n\ndef pretty_date(year, month, day):\n    return date(year, month, day).strftime(\"%d %b %Y\")\n",
    [{ args: [2026, 1, 5], expected: "05 Jan 2026" }, { args: [2026, 12, 31], expected: "31 Dec 2026" }, { args: [2000, 7, 9], expected: "09 Jul 2000" }],
    ["Build the date with `date(year, month, day)`.", "`%d` is the two-digit day, `%b` the short month, `%Y` the full year."],
    ["dates", "formatting"]),

  /* ---------- json ---------- */
  PP("json", "Medium", 310, "json-key-list", "Read the Keys", "json_keys",
    "Write a function `json_keys(text)` that reads a JSON object from `text` and returns its keys as a **sorted list**.\n\nSorting matters: without it the order is whatever the text happened to use, and the answer would not be stable.",
    [{ input: 'text=\'{"b": 1, "a": 2}\'', output: "['a', 'b']" }],
    "import json\n\ndef json_keys(text):\n    pass\n",
    "import json\n\ndef json_keys(text):\n    return sorted(json.loads(text).keys())\n",
    [{ args: ['{"b": 1, "a": 2}'], expected: ["a", "b"] }, { args: ["{}"], expected: [] }, { args: ['{"z": 1}'], expected: ["z"] }],
    ["`json.loads(text)` turns the text into a dict.", "`sorted(d.keys())` gives a stable list."],
    ["json"]),

  /* ---------- match-case ---------- */
  PP("match-case", "Medium", 311, "status-label", "Name the Status Code", "status_label",
    "Write a function `status_label(code)` using `match`/`case` that returns:\n\n- `200` -> `\"OK\"`\n- `404` -> `\"Not Found\"`\n- `500` -> `\"Server Error\"`\n- anything else -> `\"Unknown\"`",
    [{ input: "code=404", output: '"Not Found"' }, { input: "code=302", output: '"Unknown"' }],
    "def status_label(code):\n    pass\n",
    "def status_label(code):\n    match code:\n        case 200:\n            return \"OK\"\n        case 404:\n            return \"Not Found\"\n        case 500:\n            return \"Server Error\"\n        case _:\n            return \"Unknown\"\n",
    [{ args: [404], expected: "Not Found" }, { args: [200], expected: "OK" }, { args: [302], expected: "Unknown" }],
    ["`match code:` then one `case` per value.", "`case _:` is the catch-all, and it goes last."],
    ["match-case"]),

  /* ---------- inheritance ---------- */
  PP("inheritance", "Medium", 312, "override-sound", "Override the Sound", "animal_sound",
    "There is an `Animal` class whose `sound()` returns `\"...\"`. Write a `Cow` class that inherits from it and overrides `sound()` to return `\"moo\"`.\n\nThen the function `animal_sound(kind)` should return the sound for `\"cow\"`, and `Animal`'s own sound for anything else.",
    [{ input: 'kind="cow"', output: '"moo"' }, { input: 'kind="fox"', output: '"..."' }],
    "class Animal:\n    def sound(self):\n        return \"...\"\n\n# your Cow class here\n\ndef animal_sound(kind):\n    pass\n",
    "class Animal:\n    def sound(self):\n        return \"...\"\n\nclass Cow(Animal):\n    def sound(self):\n        return \"moo\"\n\ndef animal_sound(kind):\n    if kind == \"cow\":\n        return Cow().sound()\n    return Animal().sound()\n",
    [{ args: ["cow"], expected: "moo" }, { args: ["fox"], expected: "..." }, { args: [""], expected: "..." }],
    ["`class Cow(Animal):` is how you inherit.", "Defining `sound` again in Cow replaces the inherited one."],
    ["oop", "inheritance"]),

  /* ---------- encapsulation ---------- */
  PP("encapsulation", "Medium", 313, "guarded-balance", "A Balance That Refuses", "final_amount",
    "Write an `Account` class that keeps its balance in `self._balance` and has a `withdraw(n)` method which **refuses** to go below zero — if there is not enough, it changes nothing.\n\n`final_amount(start, amounts)` should start an account and try each withdrawal in turn, then return the balance left.",
    [{ input: "start=100, amounts=[30, 500, 20]", output: "50" }],
    "class Account:\n    def __init__(self, start):\n        self._balance = start\n\n    def withdraw(self, n):\n        pass\n\ndef final_amount(start, amounts):\n    pass\n",
    "class Account:\n    def __init__(self, start):\n        self._balance = start\n\n    def withdraw(self, n):\n        if n <= self._balance:\n            self._balance -= n\n        return self._balance\n\ndef final_amount(start, amounts):\n    acc = Account(start)\n    for n in amounts:\n        acc.withdraw(n)\n    return acc._balance\n",
    [{ args: [100, [30, 500, 20]], expected: 50 }, { args: [50, []], expected: 50 }, { args: [10, [20]], expected: 10 }],
    ["The guard is one `if` inside `withdraw`.", "A refused withdrawal must leave the balance exactly as it was."],
    ["oop", "encapsulation"]),

  /* ---------- dunder-methods ---------- */
  PP("dunder-methods", "Medium", 314, "cart-length", "Make len() Work", "cart_size",
    "Write a `Cart` class that holds a list of items and defines `__len__`, so that `len(cart)` returns how many items it has.\n\n`cart_size(items)` builds a cart and returns `len()` of it.",
    [{ input: 'items=["pen", "book"]', output: "2" }],
    "class Cart:\n    def __init__(self, items):\n        self.items = items\n\n# add __len__\n\ndef cart_size(items):\n    pass\n",
    "class Cart:\n    def __init__(self, items):\n        self.items = items\n\n    def __len__(self):\n        return len(self.items)\n\ndef cart_size(items):\n    return len(Cart(items))\n",
    [{ args: [["pen", "book"]], expected: 2 }, { args: [[]], expected: 0 }, { args: [["a", "b", "c"]], expected: 3 }],
    ["`__len__` must return an integer.", "Inside it, return `len(self.items)`."],
    ["oop", "dunder"]),

  /* ---------- iterators-generators ---------- */
  PP("iterators-generators", "Medium", 315, "first-evens", "Generate the First Evens", "first_evens",
    "Write a **generator** function that yields even numbers starting from 2, and a function `first_evens(n)` that returns the first `n` of them as a list.",
    [{ input: "n=3", output: "[2, 4, 6]" }, { input: "n=0", output: "[]" }],
    "def evens():\n    pass\n\ndef first_evens(n):\n    pass\n",
    "def evens():\n    value = 2\n    while True:\n        yield value\n        value += 2\n\ndef first_evens(n):\n    out = []\n    gen = evens()\n    for _ in range(n):\n        out.append(next(gen))\n    return out\n",
    [{ args: [3], expected: [2, 4, 6] }, { args: [0], expected: [] }, { args: [1], expected: [2] }],
    ["A generator uses `yield` instead of `return`, and can loop forever.", "`next(gen)` pulls one value at a time.", "Never build the whole infinite sequence - take only what you need."],
    ["generators", "iterators"]),

  /* ---------- decorators ---------- */
  PP("decorators", "Medium", 316, "double-decorator", "A Decorator That Doubles", "run_doubled",
    "Write a decorator `double` that doubles whatever the function it wraps returns.\n\n`run_doubled(n)` should apply it to a function that returns `n`, and give back the doubled value.",
    [{ input: "n=5", output: "10" }],
    "def double(fn):\n    pass\n\ndef run_doubled(n):\n    pass\n",
    "def double(fn):\n    def wrapper(*args, **kwargs):\n        return fn(*args, **kwargs) * 2\n    return wrapper\n\ndef run_doubled(n):\n    @double\n    def value(x):\n        return x\n    return value(n)\n",
    [{ args: [5], expected: 10 }, { args: [0], expected: 0 }, { args: [-3], expected: -6 }],
    ["A decorator takes a function and returns a new function.", "The inner wrapper calls the original and changes the result."],
    ["decorators", "functions"]),
  PP("decorators", "Hard", 317, "call-counter", "Count the Calls", "count_calls",
    "Write a decorator that counts how many times the function it wraps is called.\n\n`count_calls(times)` should call a decorated function `times` times and return the count the decorator recorded.",
    [{ input: "times=3", output: "3" }],
    "def counted(fn):\n    pass\n\ndef count_calls(times):\n    pass\n",
    "def counted(fn):\n    def wrapper(*args, **kwargs):\n        wrapper.calls += 1\n        return fn(*args, **kwargs)\n    wrapper.calls = 0\n    return wrapper\n\ndef count_calls(times):\n    @counted\n    def work():\n        return 1\n    for _ in range(times):\n        work()\n    return work.calls\n",
    [{ args: [3], expected: 3 }, { args: [0], expected: 0 }, { args: [1], expected: 1 }],
    ["The count has to live somewhere that survives between calls.", "An attribute on the wrapper works: `wrapper.calls = 0` after defining it.", "A `nonlocal` variable works too."],
    ["decorators", "closures"]),

  /* ---------- concurrency (no threads in the browser - the reasoning is the point) ---------- */
  PP("concurrency", "Medium", 318, "serial-time", "How Long, One After Another", "serial_time",
    "Tasks run one after another. Write a function `serial_time(durations)` that returns the total time taken.\n\nThis is the number that parallel work is trying to beat.",
    [{ input: "durations=[3, 1, 2]", output: "6" }],
    "def serial_time(durations):\n    pass\n",
    "def serial_time(durations):\n    return sum(durations)\n",
    [{ args: [[3, 1, 2]], expected: 6 }, { args: [[]], expected: 0 }, { args: [[5]], expected: 5 }],
    ["One after another means the times simply add up."],
    ["concurrency"]),
  PP("concurrency", "Medium", 319, "parallel-time", "How Long, Sharing the Work", "parallel_time",
    "Now the same tasks are shared between `workers` workers. Each worker takes the next task the moment it is free, longest tasks first.\n\nWrite `parallel_time(durations, workers)` returning when the **last** worker finishes. With 0 workers or no tasks, the answer is 0.",
    [{ input: "durations=[3, 2, 1], workers=2", output: "3" }, { input: "durations=[5], workers=4", output: "5" }],
    "def parallel_time(durations, workers):\n    pass\n",
    "def parallel_time(durations, workers):\n    if not durations or workers <= 0:\n        return 0\n    loads = [0] * workers\n    for d in sorted(durations, reverse=True):\n        i = loads.index(min(loads))\n        loads[i] += d\n    return max(loads)\n",
    [{ args: [[3, 2, 1], 2], expected: 3 }, { args: [[5], 4], expected: 5 }, { args: [[], 2], expected: 0 }],
    ["Keep a running total per worker in a list.", "`loads.index(min(loads))` finds the worker that is free earliest.", "The answer is the largest total, not the sum."],
    ["concurrency", "algorithms"]),

  /* ---------- async ---------- */
  PP("async", "Medium", 320, "finish-order", "Who Finishes First", "finish_order",
    "Several tasks start at different moments and take different lengths of time, but they all run together rather than queueing.\n\nEach task is `[name, start, duration]`. Write `finish_order(tasks)` returning the names in the order they **finish**. If two finish together, put them in alphabetical order.",
    [{ input: 'tasks=[["a", 0, 3], ["b", 1, 1]]', output: "['b', 'a']" }],
    "def finish_order(tasks):\n    pass\n",
    "def finish_order(tasks):\n    ordered = sorted(tasks, key=lambda t: (t[1] + t[2], t[0]))\n    return [t[0] for t in ordered]\n",
    [{ args: [[["a", 0, 3], ["b", 1, 1]]], expected: ["b", "a"] }, { args: [[]], expected: [] }, { args: [[["x", 0, 1], ["y", 0, 1]]], expected: ["x", "y"] }],
    ["A task finishes at `start + duration`.", "`sorted(..., key=lambda t: (finish, name))` breaks ties alphabetically."],
    ["async", "sorting"]),
  PP("async", "Medium", 321, "await-savings", "What Waiting Together Saves", "time_saved",
    "The same tasks run one after another, or all together starting at time 0. Write `time_saved(durations)` returning how much time running them together saves.\n\nTogether, they all finish when the longest one does.",
    [{ input: "durations=[3, 2, 1]", output: "3" }],
    "def time_saved(durations):\n    pass\n",
    "def time_saved(durations):\n    if not durations:\n        return 0\n    return sum(durations) - max(durations)\n",
    [{ args: [[3, 2, 1]], expected: 3 }, { args: [[]], expected: 0 }, { args: [[4]], expected: 0 }],
    ["One after another costs `sum(durations)`.", "All together costs `max(durations)`.", "A single task saves nothing, and that is the point."],
    ["async", "concurrency"]),

  /* ---------- system-modules ---------- */
  PP("system-modules", "Easy", 322, "path-pieces", "Take a Path Apart", "path_pieces",
    "Use `pathlib.PurePosixPath`. Write a function `path_pieces(path)` that returns a list of two items: the file name without its extension, and the extension.\n\nA file with no extension gives an empty string for the second item.",
    [{ input: 'path="data/sales.csv"', output: "['sales', '.csv']" }],
    "from pathlib import PurePosixPath\n\ndef path_pieces(path):\n    pass\n",
    "from pathlib import PurePosixPath\n\ndef path_pieces(path):\n    p = PurePosixPath(path)\n    return [p.stem, p.suffix]\n",
    [{ args: ["data/sales.csv"], expected: ["sales", ".csv"] }, { args: ["notes"], expected: ["notes", ""] }, { args: ["a/b/report.txt"], expected: ["report", ".txt"] }],
    ["`.stem` is the name without the extension.", "`.suffix` includes the dot, and is `\"\"` when there is none."],
    ["modules", "pathlib"]),
  PP("system-modules", "Medium", 323, "top-words", "Most Common Words", "top_words",
    "Use `collections.Counter`. Write a function `top_words(words, n)` that returns the `n` most common words as a list of `[word, count]` pairs, most frequent first.\n\nWhen two words tie, put them in alphabetical order.",
    [{ input: 'words=["a", "b", "a"], n=2', output: "[['a', 2], ['b', 1]]" }],
    "from collections import Counter\n\ndef top_words(words, n):\n    pass\n",
    "from collections import Counter\n\ndef top_words(words, n):\n    counts = Counter(words)\n    ordered = sorted(counts.items(), key=lambda kv: (-kv[1], kv[0]))\n    return [[w, c] for w, c in ordered[:n]]\n",
    [{ args: [["a", "b", "a"], 2], expected: [["a", 2], ["b", 1]] }, { args: [[], 3], expected: [] }, { args: [["x", "y"], 1], expected: [["x", 1]] }],
    ["`Counter(words)` counts them for you.", "Sort by `(-count, word)` so ties go alphabetically.", "Return pairs as lists, not as a dict."],
    ["modules", "collections"]),

  /* ---------- data-persistence ---------- */
  PP("data-persistence", "Medium", 324, "json-round-trip", "Save It as JSON", "json_round_trip",
    "Write a function `json_round_trip(records)` that writes the list to a file as JSON, reads it back, and returns how many records came back.\n\nIf the count matches what went in, the round trip worked.",
    [{ input: 'records=[{"a": 1}, {"a": 2}]', output: "2" }],
    "import json\n\ndef json_round_trip(records):\n    pass\n",
    "import json\n\ndef json_round_trip(records):\n    with open(\"records.json\", \"w\") as f:\n        json.dump(records, f)\n    with open(\"records.json\") as f:\n        return len(json.load(f))\n",
    [{ args: [[{ a: 1 }, { a: 2 }]], expected: 2 }, { args: [[]], expected: 0 }, { args: [[{ x: 9 }]], expected: 1 }],
    ["`json.dump(data, f)` writes, `json.load(f)` reads back.", "Return the length of what you read, not what you wrote."],
    ["files", "json"]),
  PP("data-persistence", "Medium", 325, "csv-total", "Total a CSV Column", "csv_total",
    "Write a function `csv_total(rows)` that writes the rows to a CSV file with the headers `name` and `amount`, reads the file back, and returns the total of the `amount` column.\n\nEach row arrives as `[name, amount]`, with the amount as text.",
    [{ input: 'rows=[["a", "10"], ["b", "5"]]', output: "15" }],
    "import csv\n\ndef csv_total(rows):\n    pass\n",
    "import csv\n\ndef csv_total(rows):\n    with open(\"data.csv\", \"w\", newline=\"\") as f:\n        writer = csv.writer(f)\n        writer.writerow([\"name\", \"amount\"])\n        for r in rows:\n            writer.writerow(r)\n    total = 0\n    with open(\"data.csv\", newline=\"\") as f:\n        for row in csv.DictReader(f):\n            total += int(row[\"amount\"])\n    return total\n",
    [{ args: [[["a", "10"], ["b", "5"]]], expected: 15 }, { args: [[]], expected: 0 }, { args: [[["x", "7"]]], expected: 7 }],
    ["`csv.writer(f).writerow(...)` writes one row at a time.", "`csv.DictReader` gives each row as a dict keyed by the header.", "The amount arrives as text - `int(...)` it before adding."],
    ["files", "csv"]),

  /* ---------- testing ---------- */
  PP("testing", "Easy", 326, "count-passing", "How Many Passed", "count_passing",
    "Each case is `[input, expected]` for a function that doubles its input. Write `count_passing(cases)` returning how many cases the doubling actually satisfies.",
    [{ input: "cases=[[1, 2], [2, 5]]", output: "1" }],
    "def count_passing(cases):\n    pass\n",
    "def count_passing(cases):\n    passed = 0\n    for value, expected in cases:\n        if value * 2 == expected:\n            passed += 1\n    return passed\n",
    [{ args: [[[1, 2], [2, 5]]], expected: 1 }, { args: [[]], expected: 0 }, { args: [[[3, 6], [4, 8]]], expected: 2 }],
    ["Unpack each case with `for value, expected in cases:`.", "Count the ones where `value * 2` matches."],
    ["testing"]),
  PP("testing", "Medium", 327, "first-failure", "Find the First Failure", "first_failure",
    "Same cases, but now return the **position** of the first one that fails, or `-1` if they all pass.\n\nA test report that says which case broke is worth far more than one that says how many.",
    [{ input: "cases=[[1, 2], [2, 5], [3, 7]]", output: "1" }],
    "def first_failure(cases):\n    pass\n",
    "def first_failure(cases):\n    for i, (value, expected) in enumerate(cases):\n        if value * 2 != expected:\n            return i\n    return -1\n",
    [{ args: [[[1, 2], [2, 5], [3, 7]]], expected: 1 }, { args: [[[1, 2]]], expected: -1 }, { args: [[]], expected: -1 }],
    ["`enumerate(cases)` gives you the position and the case together.", "Return as soon as one fails - that is what makes it the first."],
    ["testing"]),

  /* ---------- debugging-logging ---------- */
  PP("debugging-logging", "Easy", 328, "capture-warnings", "Capture the Warnings", "capture_warnings",
    "Write a function `capture_warnings(messages)` that logs each message as a **warning** and returns the formatted lines as a list, each in the form `WARNING: text`.\n\nSend the log to a `StringIO` so you can read back exactly what was written.",
    [{ input: 'messages=["bad row"]', output: "['WARNING: bad row']" }],
    "import logging\nimport io\n\ndef capture_warnings(messages):\n    pass\n",
    "import logging\nimport io\n\ndef capture_warnings(messages):\n    stream = io.StringIO()\n    logger = logging.getLogger(\"capture\")\n    logger.handlers = []\n    logger.propagate = False\n    logger.setLevel(logging.WARNING)\n    handler = logging.StreamHandler(stream)\n    handler.setFormatter(logging.Formatter(\"%(levelname)s: %(message)s\"))\n    logger.addHandler(handler)\n    for m in messages:\n        logger.warning(m)\n    return [line for line in stream.getvalue().split(\"\\n\") if line]\n",
    [{ args: [["bad row"]], expected: ["WARNING: bad row"] }, { args: [[]], expected: [] }, { args: [["a", "b"]], expected: ["WARNING: a", "WARNING: b"] }],
    ["`logging.StreamHandler(stream)` writes into your StringIO instead of the console.", "The format string `\"%(levelname)s: %(message)s\"` produces exactly this shape.", "Split the captured text on newlines and drop the empty last one."],
    ["logging", "debugging"]),
  PP("debugging-logging", "Medium", 329, "level-cutoff", "What Gets Through", "level_cutoff",
    "Each message is `[level, text]`, where level is one of `debug`, `info`, `warning`, `error`, `critical`.\n\nWrite `level_cutoff(messages, level)` returning how many messages are at that level **or above** - the same rule the logging module uses.",
    [{ input: 'messages=[["info", "a"], ["error", "b"]], level="warning"', output: "1" }],
    "def level_cutoff(messages, level):\n    pass\n",
    "def level_cutoff(messages, level):\n    order = {\"debug\": 10, \"info\": 20, \"warning\": 30, \"error\": 40, \"critical\": 50}\n    cut = order.get(level, 0)\n    return sum(1 for lv, _ in messages if order.get(lv, 0) >= cut)\n",
    [{ args: [[["info", "a"], ["error", "b"]], "warning"], expected: 1 }, { args: [[], "debug"], expected: 0 }, { args: [[["debug", "x"], ["critical", "y"]], "debug"], expected: 2 }],
    ["Give each level a number so they can be compared.", "Setting a level shows that level and everything more severe."],
    ["logging", "debugging"]),

  /* ---------- clean-code ---------- */
  PP("clean-code", "Medium", 330, "named-discount", "Name the Numbers", "final_price",
    "Write a function `final_price(price, qty)` that gives a **20% discount** when the order total reaches **5000**, and none below that. Return the final total, rounded to 2 decimals.\n\nWrite the 5000 and the 0.2 as named constants, not as bare numbers buried in an `if`.",
    [{ input: "price=1000, qty=6", output: "4800.0" }, { input: "price=100, qty=2", output: "200" }],
    "BULK_THRESHOLD = 5000\nBULK_DISCOUNT = 0.2\n\ndef final_price(price, qty):\n    pass\n",
    "BULK_THRESHOLD = 5000\nBULK_DISCOUNT = 0.2\n\ndef final_price(price, qty):\n    total = price * qty\n    if total >= BULK_THRESHOLD:\n        total = total - total * BULK_DISCOUNT\n    return round(total, 2)\n",
    [{ args: [1000, 6], expected: 4800.0 }, { args: [100, 2], expected: 200 }, { args: [5000, 1], expected: 4000.0 }],
    ["Work out the total first, then decide about the discount.", "A named constant makes the rule findable when it changes."],
    ["clean-code"]),

  /* ---------- project-git ---------- */
  PP("project-git", "Easy", 331, "running-as-script", "Script, or Imported?", "run_mode",
    "When Python runs a file directly it sets `__name__` to `\"__main__\"`. When the file is imported instead, `__name__` is the module's own name.\n\nWrite a function `run_mode(name)` that returns `\"script\"` when it is given `\"__main__\"`, and `\"imported\"` for anything else.",
    [{ input: 'name="__main__"', output: '"script"' }, { input: 'name="report"', output: '"imported"' }],
    "def run_mode(name):\n    pass\n",
    "def run_mode(name):\n    if name == \"__main__\":\n        return \"script\"\n    return \"imported\"\n",
    [{ args: ["__main__"], expected: "script" }, { args: ["report"], expected: "imported" }, { args: [""], expected: "imported" }],
    ["This is the check behind `if __name__ == \"__main__\":`."],
    ["project", "modules"]),
  PP("project-git", "Medium", 332, "safe-config", "Config That Does Not Leak", "safe_config",
    "`defaults` and `extra` both arrive as lists of `[key, value]` pairs. Build a configuration from the defaults, apply the overrides on top, and return the result as a list of `[key, value]` pairs **sorted by key**.\n\nThe catch: build it from a **copy** of the defaults. Changing the shared defaults in place is a real bug, and it is silent.",
    [{ input: 'defaults=[["rows", 100]], extra=[["rows", 500]]', output: "[['rows', 500]]" }],
    "def safe_config(defaults, extra):\n    pass\n",
    "def safe_config(defaults, extra):\n    cfg = dict(defaults)\n    cfg.update(dict(extra))\n    return [[k, cfg[k]] for k in sorted(cfg)]\n",
    [{ args: [[["rows", 100]], [["rows", 500]]], expected: [["rows", 500]] }, { args: [[["a", 1], ["b", 2]], []], expected: [["a", 1], ["b", 2]] }, { args: [[], [["x", 9]]], expected: [["x", 9]] }],
    ["`dict(pairs)` turns a list of pairs into a dict.", "`dict(DEFAULTS)` copies; `cfg = DEFAULTS` only renames.", "Return pairs sorted by key so the answer is stable."],
    ["project", "dicts"]),

  /* ---------- statistics: the four topics still short of two ---------- */
  PP("bayes", "Medium", 340, "positive-test-chance", "What a Positive Test Means", "posterior",
    "A screening test is used on a whole population. Write `posterior(prev, sens, spec)` returning the probability that someone who tested **positive** is genuinely ill, rounded to 4 decimal places.\n\n- `prev` — the share of the population who are ill\n- `sens` — the share of ill people the test correctly flags\n- `spec` — the share of healthy people the test correctly clears\n\nRemember there are **two** roads to a positive result: an ill person correctly flagged, and a healthy person wrongly flagged.",
    [{ input: "prev=0.01, sens=0.99, spec=0.95", output: "0.1667" }, { input: "prev=0.5, sens=0.99, spec=0.95", output: "0.9519" }],
    "def posterior(prev, sens, spec):\n    pass\n",
    "def posterior(prev, sens, spec):\n    p_pos = sens * prev + (1 - spec) * (1 - prev)\n    return round(sens * prev / p_pos, 4)\n",
    [{ args: [0.01, 0.99, 0.95], expected: 0.1667 }, { args: [0.5, 0.99, 0.95], expected: 0.9519 }, { args: [0.02, 0.99, 0.95], expected: 0.2878 }],
    ["The false-alarm road is (1 - spec) multiplied by the healthy share, which is (1 - prev).", "Denominator = sens * prev + (1 - spec) * (1 - prev).", "Then divide the true-positive road by that total."],
    ["bayes", "stats"]),

  PP("distributions", "Medium", 341, "binomial-exact", "Exactly k Successes", "exactly_k",
    "Write `exactly_k(n, k, p)` returning the probability of getting **exactly** k successes in n independent tries, each succeeding with probability p. Round to 4 decimal places.\n\nUse `math.comb`, not `math.perm` — a binomial asks how many succeeded, never in which order.",
    [{ input: "n=10, k=3, p=0.5", output: "0.1172" }, { input: "n=5, k=2, p=0.5", output: "0.3125" }],
    "import math\n\ndef exactly_k(n, k, p):\n    pass\n",
    "import math\n\ndef exactly_k(n, k, p):\n    return round(math.comb(n, k) * p**k * (1 - p)**(n - k), 4)\n",
    [{ args: [10, 3, 0.5], expected: 0.1172 }, { args: [5, 2, 0.5], expected: 0.3125 }, { args: [10, 5, 0.5], expected: 0.2461 }],
    ["The formula is C(n,k) * p**k * (1-p)**(n-k).", "math.comb(n, k) counts the ways to choose which k succeeded.", "The (n - k) failures each contribute (1 - p)."],
    ["binomial", "stats"]),

  PP("sampling-clt", "Medium", 342, "standard-error-of", "How Precise Is This Mean?", "standard_error",
    "Write `standard_error(values)` returning the standard error of the mean, rounded to 4 decimal places.\n\nThat is the **sample** standard deviation (dividing by n-1) divided by the square root of n. It describes how far the sample mean would sit from the true mean across repeated samples — which is not the same as how spread out the values themselves are.",
    [{ input: "values=[2, 4, 6, 8, 10]", output: "1.4142" }, { input: "values=[1, 2, 3]", output: "0.5774" }],
    "import math\n\ndef standard_error(values):\n    pass\n",
    "import math\n\ndef standard_error(values):\n    n = len(values)\n    mean = sum(values) / n\n    var = sum((v - mean) ** 2 for v in values) / (n - 1)\n    return round(math.sqrt(var) / math.sqrt(n), 4)\n",
    [{ args: [[2, 4, 6, 8, 10]], expected: 1.4142 }, { args: [[1, 2, 3]], expected: 0.5774 }, { args: [[4, 8]], expected: 2.0 }],
    ["Divide the squared deviations by n-1, not n — this is a sample.", "Take the square root of that to get the standard deviation.", "Then divide by math.sqrt(n). Forgetting this last step is the classic mistake."],
    ["sampling", "stats"]),

  PP("hypothesis-testing", "Medium", 343, "two-tailed-p", "The Two-Tailed p-value", "two_tailed_p",
    "Write `two_tailed_p(z)` returning the two-tailed p-value for a z-score, rounded to 4 decimal places.\n\nUse `math.erf` for the normal CDF: the share of a standard normal below z is `0.5 * (1 + erf(z / sqrt(2)))`.\n\nTwo-tailed means a result this extreme **in either direction**, so a negative z must give the same answer as its positive twin.",
    [{ input: "z=1.96", output: "0.05" }, { input: "z=2", output: "0.0455" }],
    "import math\n\ndef two_tailed_p(z):\n    pass\n",
    "import math\n\ndef two_tailed_p(z):\n    left = 0.5 * (1 + math.erf(abs(z) / math.sqrt(2)))\n    return round(2 * (1 - left), 4)\n",
    [{ args: [1.96], expected: 0.05 }, { args: [2], expected: 0.0455 }, { args: [0], expected: 1.0 }, { args: [-2], expected: 0.0455 }],
    ["Take abs(z) first, so the sign cannot change the answer.", "The area to the right of |z| is 1 minus the CDF.", "Double it, because the test counts both tails."],
    ["hypothesis", "stats"]),
];

export { topicProblems };
